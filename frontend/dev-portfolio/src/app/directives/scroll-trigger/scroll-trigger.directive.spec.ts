import { ScrollTriggerDirective } from './scroll-trigger.directive';

describe('ScrollTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
